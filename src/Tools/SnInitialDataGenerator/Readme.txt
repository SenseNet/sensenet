The "*.ps1" files are source files in this directory. Use powershell scripts in the runtime directory for example:
* CD bin\Release\net6.0 
* CreateTestDatabase.ps1

The scripts overwrites 3-3 source files. The temporary directory is the "output" under the runtime directory.
